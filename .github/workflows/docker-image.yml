name: Docker Image CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      APP_IMAGE: ghcr.io/albin-rohde/no-name_app
      APP_BASE_URL: ''
    steps:
    - name: check out code
      uses: actions/checkout@v2

    - name: Login to Github Packages
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.CR_PAT }}

    - name: Set up Docker Buildx
      uses: crazy-max/ghaction-docker-buildx@v1
      with:
        version: latest


    - name: Pull images
      run: |
        docker pull ${APP_IMAGE}:latest

    - name: Build and Push app
      run: |
        docker buildx build \
        -t ${APP_IMAGE}:${GITHUB_SHA::7} \
        -t ${APP_IMAGE}:latest \
        -f ./Dockerfile \
        --cache-from ${FRONTEND_IMAGE}:latest \
        --build-arg REACT_APP_API_BASE_URL=${APP_BASE_URL}/api \
        --build-arg REACT_APP_SOCKET_BASE_URL=${APP_BASE_URL} \
        --build-arg REACT_APP_CLIENT_URL=${APP_BASE_URL} \
        --platform=linux/amd64 \
        --output type=image,push=true \
        ./
