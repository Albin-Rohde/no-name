import { Router, Request } from 'express'
/*
const gameSocket = Router()

gameSocket.ws('/lobby/:key', async(ws, req: Request) => {
  ws.on('connection', (data) => {
    console.log(data)
    ws.send(data)
  })
})
*/
